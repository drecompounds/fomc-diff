# secrets — never commit a token, ever. FRED is keyless; annotate.py reads env only.
.env
.env.*
*.key
*_token.json
credentials.json

# raw HTML cache — reproducibility comes from data/manifest.csv (sha256), not from these
data/raw/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
